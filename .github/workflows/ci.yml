name: CI

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            compiler: g++
            compiler_c: gcc
            name: Linux (gcc)
            build_dir: build
            exe_suffix: ""
          
          - os: ubuntu-latest
            compiler: clang++
            compiler_c: clang
            name: Linux (clang)
            build_dir: build
            exe_suffix: ""
            install_cmd: sudo apt-get update && sudo apt-get install -y clang
          
          - os: windows-latest
            compiler: msvc
            name: Windows (MSVC)
            build_dir: build/Release
            exe_suffix: .exe

    runs-on: ${{ matrix.os }}
    name: ${{ matrix.name }}
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Install compiler dependencies
        if: matrix.install_cmd != ''
        run: ${{ matrix.install_cmd }}
      
      - name: Configure CMake
        run: |
          if [ "${{ runner.os }}" = "Windows" ]; then
            cmake -B build
          else
            cmake -B build -DCMAKE_CXX_COMPILER=${{ matrix.compiler }}
          fi
        shell: bash
      
      - name: Build
        run: |
          if [ "${{ runner.os }}" = "Windows" ]; then
            cmake --build build --config Release
          else
            cmake --build build
          fi
        shell: bash
      
      - name: Run hello_world
        run: |
          if [ "${{ runner.os }}" = "Windows" ]; then
            ./build/Release/hello_world${{ matrix.exe_suffix }}
          else
            ./${{ matrix.build_dir }}/hello_world${{ matrix.exe_suffix }}
          fi
        shell: bash
      
      - name: Run solver
        run: |
          if [ "${{ runner.os }}" = "Windows" ]; then
            ./build/Release/solver${{ matrix.exe_suffix }}
          else
            ./${{ matrix.build_dir }}/solver${{ matrix.exe_suffix }}
          fi
        shell: bash
